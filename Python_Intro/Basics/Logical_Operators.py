# and   &&
# or    ||
# not   ! 
# < 
# >
# >=
# <=
# !=
# << Shift left
# >> Shift Right
# Also has other bitwise ops, |, &, etc
# is => Example: a is b = True IF a is the same obj as b
# == equality
# in => Example: a in b = True if a is in the iterable b