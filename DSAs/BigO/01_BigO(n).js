/*

Big O of n -> O(n) is linear time.

Time increases linearly when you increase the inputs.

1000 inputs will run roughly 2x faster than the same function with 2000 inputs

Indexing an array is a simple example of O(n)

O(n) is prob the most common

*/

